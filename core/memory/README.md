# Memory Core

Desktop-owned memory logic will live here.

Existing Memoria code is a reference and future import source. This module should read and write the new `claracore.db` schema, not the current local Memoria runtime database.
