# 0.7.6 — 紧凑运行状态

2026-09-11，个人 Mac arm64 Lite，未公开发布。

针对用户截图中运行状态冗长、缺少层次的问题，调整现有「数据与恢复」中的展示：

- 备份、记忆向量、MCP 三张紧凑卡片；短标签、状态徽标、关键数值和向量覆盖条。
- 宽度充足时并排，空间不足时自动换行；使用已有明暗主题颜色，不固定卡片高度或截断内容。
- 完整时间和诊断解释放入默认折叠的「查看时间与诊断详情」。保留原始信息及“历史验证不代表当前在线”等边界。
- 备份失败、清理待处理、搜索降级和向量缺失仍在卡片下方直接提示；未搜索、旧 MCP 协议本身不显示为错误。
- 定时刷新保留原生 details/summary 节点，维持展开状态与键盘焦点；只有异常区域做状态播报。

未改变备份、向量或协议业务处理。`npm run check`、`git diff --check` 和表单/状态渲染逻辑测试通过，覆盖三张卡片、异常与中性状态、目录变更、未保存编辑和稳定的详情节点。

本次未操作用户界面或浏览器；截图用于问题定位，实际视觉、窄窗口和深色主题效果仍待用户安装验收。

## 本地交付状态

实际 ASAR 中的布局、样式、中英文文案和入口与已验证源码逐字一致，Lite 检查通过（展开约 294.1 MiB）。App 与 DMG 均已完成 Developer ID 签名、Apple 公证和附票，Gatekeeper 均 accepted / Notarized Developer ID。

产物：`dist-lite/ClaraCore-Desktop-0.7.6-lite-arm64.dmg`。
SHA-256：`c76bbd2af865cc8ff3873a13f064e7e575512257583495da1473538b5f43bd32`。
App 公证 ID：`e287d676-403f-4479-92c6-90568d125135`。
DMG 公证 ID：`f73b9726-2ca9-41f2-8538-53d7584736c5`。

公证首次在本机读取 `claracore-notary` profile 时失败，报 No Keychain password item found；并非 Apple 拒绝审核。后续同一 profile 的 history 查询恢复成功，无需重新配置凭据，随后完成公证。此前“需要恢复凭据”的判断过强：已确认的事实只是阶段性读取失败，具体原因未定位，不能据此断言凭据被删除或钥匙串损坏。未修改签名/认证配置。

未替用户安装、未提交推送或公开发布；视觉与交互仍待用户确认。
